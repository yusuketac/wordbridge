# WordBridge UIデザイン概要

## 製品概要

- アプリ名: WordBridge
- 一行説明: 敬語変換、ビジネス文作成、翻訳、学習を通じて、意図と相手をつなぐコミュニケーションアプリ。
- 主な利用者: 敬語やビジネス文章に迷いがあり、社内外の連絡を短時間で整えたいWindowsユーザー。
- 最も重要な操作: 起動直後に文章を貼り付け、宛先を選び、敬語へ変換してコピーする。
- 利用頻度: 業務中に1日数回、短時間で繰り返し利用。
- 対象プラットフォーム・ホスト: Windowsデスクトップのブラウザ。単一HTML、ビルド不要、オフラインでUI起動。
- UIフレームワークと選定理由: Vanilla HTML/CSS/JavaScript。既存の配布性、起動速度、データ互換性を維持するため。

## 利用環境

- モニターと配置: Windowsマルチモニター。メイン画面またはサブ画面の小さめウィンドウ。
- 主な入力方法: キーボードとマウス。貼り付け、ショートカット実行、コピー中心。
- 通常ウィンドウサイズ: 1180 × 780px。
- コンパクトサイズ: 900 × 680px。
- 最小実用サイズ: 680 × 620px。
- 画面スケーリングへの配慮: 125〜150%でもボタンや長い日本語が折り返し、横スクロールを出さない。
- 起動速度・動作速度の目標: HTMLを開いて1秒以内に入力可能。AI処理中だけ対象ボタンを無効化し、完了時に実測秒数を表示する。短文変換はFlash-Lite・最小思考を既定とする。

## 参考UIの選定

| 画像ファイルまたはURL | 取り入れる要素 | コピーしない要素 |
|---|---|---|
| `ui-reference-007.png`（Papago） | 入力から変換へ迷わず進める単一の強い主操作、用途選択の明快さ | モバイル向けの大きすぎる余白、ブランドカラー、音声・画像機能 |
| `ui-reference-009.png`（翻訳） | 原文と結果の分離、結果からコピーへ進む短い導線 | 対面会話レイアウト、スマホ下部ナビゲーション、ターコイズの全面使用 |
| `ui-reference-027.png`（Craft） | 文書作業と学習カードを同じ製品内に収める情報階層 | 写真・グラデーション中心の装飾、モバイルカードの大きさ |
| `ui-reference-006.png`（リマインダー） | 件数つきの要チェック一覧、項目をすぐ完了へ移せる操作 | 明るいカード配色、モバイルの大きな余白、下部ナビゲーション |
| `ui-reference-031.png`（ヘルスケア） | 弱点を短い数値サマリーで把握する見せ方 | 健康情報らしいグラフ、白基調、モバイル画面の比率 |

## デザイン方針

- 視覚的なトーン: 濃紺とスレートを基調にした、落ち着いたデスク/HMI風。装飾より読みやすさを優先。
- 情報密度: 中〜高密度。主操作は余裕を持たせ、補助情報はコンパクトにまとめる。
- 情報の優先順位: 入力と変換 > 結果とコピー > 学習ポイント > 保存・設定。
- ナビゲーション方法: デスクトップは左サイドバー、680px以下では上部横スクロールタブ。
- 最重要アクション: 「変換」。起動時に入力欄へフォーカスする。
- 補助アクション: 文書作成、校正、翻訳、フレーズ登録、学習、設定。
- コーチング操作: 校正で確認した傾向を「観察中」から「要チェック」へ自動整理し、改善できたらユーザー自身で卒業させる。相談はEnterで実行し、会話本文は保存しない。
- このアプリ独自の視覚的な特徴: 「原文 → 整えた文章」を上下または左右で明確に分けるワード・ブリッジ。

## 名前とアイコン

- 採用名と理由: `WordBridge`。敬語だけでなく、文書作成・翻訳・学習まで扱う広さを保ち、「自分の意図を相手へ正しく渡す」という目的を平易な英単語で表せる。
- アプリアイコンのコンセプト: 角丸の濃紺タイル内に、水色の吹き出しを1つだけ大きく置く。
- 小サイズでも認識できる工夫: 中央モチーフ1つ、2色、太いシルエット、文章線を2本に限定する。
- 編集可能なアイコン元データ: `assets/wordbridge-icon.svg`。同じ形をHTML内にも埋め込み、単一HTMLだけでもfaviconとブランド表示が機能する。
- DEVBOARD用アイコン: `assets/wordbridge-icon.png`。DEVBOARDが読み込める512KB以下の軽量PNGとして生成する。
- パッケージへ含めるアイコン形式: 単一HTMLでは埋め込みSVG faviconを使用。将来パッケージ化する際はSVGからICOを生成する。

## デザイントークン

### 色の役割

| 役割 | 値 | 用途 |
|---|---|---|
| 背景 | `#0B1018` | アプリ全体 |
| サーフェス | `#131B27` / `#182231` | サイドバー、カード、入力欄 |
| アクセント | `#38BDF8` | 主要操作、選択、フォーカス |
| 主要テキスト | `#F2F6FA` | 見出し、本文 |
| 補助テキスト | `#91A0B4` | ヒント、説明 |
| 境界線 | `#2A3748` | カード、入力、区切り |
| 成功 | `#34D399` | 完了、良い状態 |
| 警告 | `#FBBF24` | 機密注意、設定不足 |
| エラー | `#FB7185` | エラー、削除 |

### 文字と形状

- フォントとフォールバック: Segoe UI、Hiragino Sans、Meiryo、sans-serif。
- 文字サイズ体系: 11 / 12 / 13 / 14 / 18 / 24px。
- 文字ウェイト: 400、600、700。
- 余白体系: 4px単位。8、12、16、24pxを中心に使用。
- コントロールの高さ: 32〜40px。主ボタンは40px。
- 角丸: 8px（入力・ボタン）、12px（カード）、16px（大領域）。
- 境界線と奥行き表現: 1px境界線。影は最小限で、面の明度差を優先。
- アイコンファミリー、線幅、標準サイズ: インラインSVG、1.8px、18〜20px。絵文字は使わない。

## 画面・コンポーネント構成

- メイン画面の領域: 左ナビゲーション、ページ見出し、入力カード、結果カード、補助学習カード。
- ページ見出し: 入力作業より目立たない18px・中ウェイトとし、説明を含めてコンパクトに配置する。
- AI入力カード: 本文とは別に任意の「前提条件」を設け、相手・話題・利用場面を短く指定できる。
- 頻繁に使う操作: 敬語変換、宛先選択、コピー、文書作成、校正。
- 低頻度操作とメニュー位置: バックアップ、モデル、API設定は「設定」へ集約。
- リスト、表、ツリー、カード: フレーズと学びは密度の高い行リスト。マナーは場面別カード。
- コーチ: 要チェック・観察中・改善済みを3つの小さな数値で要約し、改善項目は短い理由と次の一歩を表示する。
- 検索と絞り込み: フレーズ、学び、マナーを即時検索。学びはタグ絞り込みを継続。
- 空状態: 次に何をすればよいかを一文で提示。
- 読み込み・進行状態: スピナー、処理中ラベル、対象ボタン無効。一時的なAPIエラーは再試行回数と待ち秒数を表示し、長時間待たせず1回の再試行で終了する。
- 成功、警告、エラーの通知: テキストと色を併用し、1.5〜4秒で戻す。

## 操作とアクセシビリティ

- キーボード操作とショートカット: 敬語変換・文書作成・翻訳・校正はEnterで実行、Shift+Enterで改行。IME変換確定中は実行しない。タブ移動可能。
- フォーカスの視認性: 2pxの水色アウトライン。
- ホバー、押下、選択、無効状態: 明度・境界線・ラベルを併用。無効時は透明度を下げる。
- ツールチップ方針: アイコンのみの操作には必ずtitleまたはaria-labelを付ける。
- コントラストと色以外の状態表現: 選択は背景色に加えて境界線と文字ウェイトで示す。
- 長い日本語と省略表示の方針: 本文は折り返す。ナビラベルは省略しない。

## 見た目の完成条件

- [x] 目的と最重要操作がすぐに理解できる。
- [x] 参考UIの良さが、ブランドをコピーせず画面へ反映されている。
- [x] 通常、コンパクト、最小サイズのすべてが意図的で使いやすい。
- [x] 文字切れ、重なり、意図しないリサイズが残っていない。
- [x] 余白、文字、コントロール、アイコン、状態表現が統一されている。
- [x] 空、データあり、無効、読込、成功、警告、エラー状態が機能する。
- [x] キーボードフォーカスと主要ショートカットが使える。
- [x] ブラウザタブのアイコンが小サイズでも判別できる。
- [x] 実際のアプリを目視確認し、実装後の見た目を修正した。

## 2026-08-29 視覚検証

- 対象: 1180 × 780px、900 × 680px、680 × 620px。
- 確認内容: `WordBridge`の1行表示、敬語変換画面、503自動再試行表示、無効ボタン、レスポンシブナビゲーション。
- アイコン: 旧版と新しい`WordBridge`版を24px・16px実寸で比較。`assets/wordbridge-icon-size-preview.png`に記録。

## 2026-08-31 コーチ画面の視覚検証

- 参照: `ui-reference-006.png`（リマインダー）の件数つき一覧、`ui-reference-031.png`（ヘルスケア）の簡潔なサマリーを、濃紺のデスクトップUIへ翻案した。
- 対象: 通常幅、900 × 680px、680 × 620px。
- 確認内容: 要チェック・観察中・改善済みの3項目サマリー、空状態、相談候補、Enter実行の案内、横方向のはみ出しなし。
- 制限: 実際のAI応答はユーザーのAPIキーを使わずに検証していない。表示・操作導線とJavaScript構文は確認済み。
